import React from 'react';

type Props = {};

const SubscriptionToken = (props: Props) => {
  return (
    <div style={{color: '#ffffff'}}>Subscription Token</div>
  );
};

export default SubscriptionToken;