import React from 'react';

type Props = {
  title : string,
  courseName : string,
};



const Courses = (props: Props) => {
  return (
    <div style={{color: '#ffffff'}}>Courses</div>
  );
};

export default Courses;