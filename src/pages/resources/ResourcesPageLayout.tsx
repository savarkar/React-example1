import React from 'react';
import { Outlet } from 'react-router-dom';

type Props = {};

const ResourcesPageLayout = (props: Props) => {
  return (
    <><Outlet /></>
  );
};

export default ResourcesPageLayout;